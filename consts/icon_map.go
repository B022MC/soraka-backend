package consts

var (
	ItemIconMap    = make(map[int]string)
	ChampIconMap   = make(map[int]string)
	SpellIconMap   = make(map[int]string)
	ProfileIconMap = make(map[int]string)
	MapIcon        = make(map[int]string)
)
